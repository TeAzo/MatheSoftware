 private static double min(int anzahl) throws IOException {
        double[] werte = new double[anzahl];

        double min = 0;
        for (int i = 0; i < werte.length; i++) {
            min = Math.min(werte[i], min);

        }
        return min;
